# Minimal subset of ai-toolkit deps for SDXL/FLUX LoRA training on Colab.
# Skips ai-toolkit's own requirements.txt (it pins scipy==1.12 which breaks
# Colab's preinstalled numpy 2.x stack). Skips audio (librosa, torchcodec,
# av, mutagen), UI (gradio), watermark, tensorboard, k-diffusion — none are
# touched by the SDXL LoRA training path.
#
# We do NOT pin numpy/scipy here — Colab's preinstalled versions work, and
# pinning back triggers ABI mismatches.

# Diffusion stack
diffusers
transformers
accelerate
peft

# Tokenizers / language
sentencepiece
huggingface_hub
hf_transfer

# Storage formats
safetensors
pyyaml
oyaml
toml
omegaconf
flatten_json
python-dotenv

# LoRA implementations
lycoris-lora

# Image ops
albumentations
albucore
einops
kornia
opencv-python
Pillow
invisible-watermark

# Optimizers / models
bitsandbytes
prodigyopt
timm
open_clip_torch
lpips
torchao

# Quantization (needed for FLUX.1-dev)
optimum-quanto

# Utilities
python-slugify
pydantic

# ai-toolkit unconditionally imports toolkit.audio.album_artwork at FLUX/SD
# training time, which requires PyAV. We don't actually use audio features.
av
