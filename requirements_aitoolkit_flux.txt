# FLUX LoRA training via Hugging Face's official example script
# (examples/dreambooth/train_dreambooth_lora_flux.py).
# We bypass ai-toolkit entirely — its bleeding-edge deps fight Colab's
# numpy 2.x stack. This list is what HF's example actually needs.

diffusers
transformers
accelerate
peft
safetensors
huggingface_hub
sentencepiece
hf_transfer
prodigyopt
bitsandbytes

# FLUX needs quantization to fit weights in <40 GB during training.
optimum-quanto

# Image preprocessing
Pillow

# We DON'T pin numpy/scipy here — Colab's preinstalled numpy 2.x works with
# stable diffusers, no ABI mismatch.
