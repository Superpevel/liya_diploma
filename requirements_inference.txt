# Minimal deps for inference notebooks (06, 07).
# Just what we need to load SDXL/FLUX + apply LoRA + compute metrics.

diffusers
transformers
accelerate
peft
safetensors
huggingface_hub
hf_transfer
sentencepiece

# Image / metric stack
Pillow
requests
